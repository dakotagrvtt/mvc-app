<br>
<br>
<div class="container-fluid">
  <div class=row>
    <div class="col col-md-12">

      <h2>Create a new teacher </h2>
      <br>
      <% 
      maxID=0; 
      for (var i=0; i < teachers.length; i++) {
        if(teachers[i]._id > maxID) { 
          maxID=teachers[i]._id;  
        }
      }
      const teacher = {}
      teacher._id = maxID+1
     
      %>
      <div class="content">
        <div>
          <form name="teacher" method="POST" action="/teacher/save">

            <div class="form-group" id=frm>
              <label for="_id">ID</label>
              <input type="number" class="form-control" id="_id" name="_id" value="<%= maxID+1 %>" required readonly>
            </div>

            <!-- COMMON TO EDIT AND CREATE -->
            <%- include ('partial_edit') %>

            <div class="form-group">
              <input type="submit" value="Save" class="btn btn-primary" />
            </div>
          </form>

          <form method="get" action="/teacher">
            <input type="submit" value="Return to list" class="btn btn-caution" />
          </form>

        </div>
      </div>
    </div>
  </div>
</div>